/// Minimal public contract placeholder for Phase 1.
/// Keep these names aligned with the JavaScript v4 schema.
class MotionPathProject {
  const MotionPathProject({required this.schemaVersion});

  final int schemaVersion;
}
