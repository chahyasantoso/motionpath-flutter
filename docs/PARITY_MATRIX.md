# Cross-repository parity matrix

Source fixture: `packages/motionpath_core/test/fixtures/motionpath_parity_fixtures.json`, generated by `chahyasantoso/motionpath` PR #64 on 2026-07-31. The generator samples normalized progress at `0`, `0.25`, `0.5`, `0.75`, and `1`, with numeric tolerance `1e-9`.

| JS case | Flutter coverage | Comparison rule | Status |
|---|---|---|---|
| `easing` | `js_parity_fixture_test.dart` | numeric, `1e-9` | covered |
| `transformsAndColors` | `js_parity_fixture_test.dart` | numeric, colors normalized to ARGB | covered |
| `filters` | `js_parity_fixture_test.dart` | numeric, `1e-9` | covered |
| `imageSequence` | `js_parity_fixture_test.dart` | frame URL normalized to frame name | covered |
| `observationGraph` | `js_parity_fixture_test.dart` | numeric, `1e-9` | covered |
| `lifecycle` | existing lifecycle suites | behavioral, not sampled | partial |

The JS and Flutter renderer contracts intentionally serialize colors and image frames differently: JS emits CSS strings, while Flutter core emits packed ARGB values and frame names. The test normalizes only those renderer-boundary representations; it does not loosen numeric comparison or ignore missing keys.
